@using Kentico.Xperience.AiraUnified;

@model Kentico.Xperience.AiraUnified.AssetUploader.AssetsViewModel

@{
    Layout = "_AiraLayout";
    var baseUrl = Context.Request.GetBaseUrl();
}

@section styles {
}

<div id="assets-app"
    data-aira-unified-base-url="@Model.PathBase"
    data-base-url="@baseUrl"
    data-allowed-file-extensions-url="@Model.AllowedFileExtensionsUrl"
    data-select-files-button="@Model.SelectFilesButton"
    data-upload-successful-message="@Model.FilesUploadedMessage"
    data-upload-url="@Model.UploadUrl"
    data-navigation-url="@Model.NavigationUrl"
    data-navigation-page-identifier="@Model.NavigationPageIdentifier"
>
</div>